# Server Configuration (Optional - has defaults)
# PORT=3000
# HOST=0.0.0.0
# NODE_ENV=development

# Logging Configuration (Optional - has defaults)
# LOG_LEVEL=3 (0=trace, 1=trace, 2=debug, 3=info, 4=warn, 5=error, 6=fatal)
# LOG_FORMAT=pretty (pretty=human-readable with colors, json=structured JSON for Docker/monitoring)

# Solana Configuration (Optional - has defaults)
# SOLANA_RPC_URL=http://localhost:8899

# Worker Wallet (Optional - worker runs without it)
# WORKER_PRIVATE_KEY=your_base58_encoded_private_key_here

# Production deployment examples:
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# SOLANA_RPC_URL=https://api.devnet.solana.com